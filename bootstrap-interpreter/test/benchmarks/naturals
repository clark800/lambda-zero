#!/bin/bash -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CODE='main(input) := showIntegerList(naturals.take(10000)) ++ "\n"'
time "$DIR/../../main" -t <(cat "$DIR/../../../libraries/operators.zero" "$DIR/../../../libraries/prelude.zero" <(echo "$CODE"))
