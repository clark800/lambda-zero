#!/bin/bash -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CODE='main(input) := primes.take(1000).showList(showNatural) ++ "\n"'
ulimit -s unlimited
\time "$DIR/../../main" -t <(cat "$DIR/../../../libraries/operators.zero" "$DIR/../../../libraries/prelude.zero" <(echo "$CODE"))
