#* table.zero

newTable((=<), entries) := newProtoset((=<).on(first), entries)
table.lookup(key) := table.elicit((key, undefined)).mapJust(second)
table.hasKey(key) := (key, undefined) in table
table.get(key) := table.lookup(key) ?? undefined
table.set(key, value) := table.add((key, value))
table.getKeys := getElements(table).map(first)
table.getValues := getElements(table).map(second)

newDictionary := newTable((<*=))

#*
