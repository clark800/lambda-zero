#* table.zero

newTable(hash, (=<), entries) := newProtoset(hash <> first, (=<), entries)
lookup(table, key) := table.elicit((key, undefined)).mapJust(second)
hasKey(table, key) := (key, undefined) in table
get(table, key) := table.lookup(key) ?? undefined
set(table, key, value) := table.add((key, value))
getKeys(table) := getElements(table).map(first)
getValues(table) := getElements(table).map(second)

newDictionary := newTable(id, (<*=))

#*
