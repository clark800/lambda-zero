#* type.zero

Type ::= {
    MetaVariable(_ : Tag, _address : ℕ),
    Implication(_ : Tag, _parameterType : Type, _bodyType : Type),
    Construction(_ : Tag, _parameterTypes : List(Type), _count : ℕ, _index : ℕ)
}

define getTypeTag
    MetaVariable(tag, _) -> tag
    Implication(tag, _, _) -> tag
    Construction(tag, _, _, _) -> tag

Scheme ::= {Scheme(getUniversals : List(Type), getType : Type)}
