id(x) := x

const(x) := _ -> x

true(t, f) := t
false(t, f) := f

f(x, y) := y(x)

f'(x, y) := x(y)

g(x) := x * x

h(x, y) := x > y

v(x) := true

if(c, a, b) := c(a, b)

c(a) := if(a = 0, 1, a)

p := (1, true, c(1))

q(x, y) := (x, y)
